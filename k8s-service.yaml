apiVersion: v1
kind: Service
metadata:
  name: notes-api-service
  labels:
    app: notes-api
spec:
  selector:
    app: notes-api
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  type: LoadBalancer
